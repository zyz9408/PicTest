# PicTest

GitHub Pages image loading demo.

The page fetches `images.json`, then requests each image with `fetch()` and renders the returned Blob in the gallery.

After pushing to `main`, the GitHub Actions workflow deploys the site to GitHub Pages:

```text
https://zyz9408.github.io/PicTest/
```
